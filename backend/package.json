{
  "name": "kara-backend",
  "version": "0.1.0",
  "private": true,
  "scripts": {
    "dev": "npm run build && node --watch dist/index.js",
    "dev:once": "npm run build && node dist/index.js",
    "build": "tsc",
    "start": "node dist/index.js"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.106.2",
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2"
  },
  "devDependencies": {
    "@types/cors": "^2.8.17",
    "@types/express": "^5.0.0",
    "@types/node": "^20",
    "typescript": "^5"
  }
}

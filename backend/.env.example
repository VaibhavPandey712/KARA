PORT=4000
# Comma-separated list of allowed frontend origins (production + local)
FRONTEND_URL=http://localhost:3000

SUPABASE_URL=
SUPABASE_ANON_KEY=

# Optional — bypasses RLS for admin DB access
# SUPABASE_SERVICE_ROLE_KEY=
